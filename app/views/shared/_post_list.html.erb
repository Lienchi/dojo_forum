<div class="col-md-12">

  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>
          <span class="glyphicon glyphicon-tasks"></span>
          <%= sort_link(@q, :replies_count, default_order: :desc) %>
        </th>
        <th>
          <span class="glyphicon glyphicon-list-alt"></span>
          <%= sort_link(@q, :title, default_order: :desc) %>
        </th>
        <th>
          <span class="glyphicon glyphicon-time"></span>
          <%= sort_link(@q, :last_replied_at, default_order: :desc) %>
        </th>
        <th>
          <span class="glyphicon glyphicon-eye-open"></span>
          <%= sort_link(@q, :viewed_count, default_order: :desc) %>
        </th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
      <tr>
        <th scope="row"><%= post.replies_count %></th>
        <td><%= link_to post.title, post_path(post) %></td>
        <td><%= post.last_replied_at %></td>
        <td><%= post.viewed_count %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <div class="text-center">
    <%= paginate @posts %>
  </div>

</div>